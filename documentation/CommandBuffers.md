Command buffers are bundles of high level GPU instructions that are submited to the queue that need to be executed for each frame. We use a bundle or a buffer to send all the information in a single pass instead of sending commands one at a time because that back and forth between the CPU and GPU would kill performance. It would need to send each command for one frame and we would be wasting valuable time.