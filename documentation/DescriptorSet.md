To hold the references to our uniform buffers, we need a concrete structure describing the type of resources and an enumeration of each one. That structure is a descriptor set and the description of its structure is the layout. We define once the layout at startup and this enables the driver to optimize around it. After that we can fill the buffers and enumerate each one with a unique index called the binding point.