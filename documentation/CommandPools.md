The command pool is a pre allocated block of memory specifically made for command buffers. Allocating and freeing memory through the OS is expensive, thats why we reserve the space upfront. Command pools also need the specific queue family since command buffers from a pool can only be submitted to queues of the same family. The GPU also needs to know it upfront so it can format the commands to the specific queue type.