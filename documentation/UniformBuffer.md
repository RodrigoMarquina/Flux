While the vertex buffer holds the actual shape of the geometry relative to its center, the uniform buffer holds the transformation, the position relative to the world, the rotation, the scale. The vertex shader takes the vertex position from the vertex buffer and multiplies it by the transformation matrix from the uniform buffer to finally get the final screen position. The uniform buffer is how the program logic talks to the shader every frame. 